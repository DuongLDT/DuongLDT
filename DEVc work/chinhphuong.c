#include<stdio.h>
#include <math.h>
/*int main(){
	int i, n;
	printf ("Nhap vao n so duong: ");
	scanf ("%d", &n);
	for (i=1;i<=sqrt(n);i++)
	printf ("%d ",i*i);
	return 0;
}*/
int main(){
	int check(int n);
	int flag = 0;
	if (sqrt(n) * sqrt(n) == n)
	 flag=1;
	return flag;
}
